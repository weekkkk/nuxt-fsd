import type { TLayers } from "../types";

export const DEFAULT_LAYERS: TLayers = {
  shared: {},
  entities: {
    hasSlices: true,
  },
  features: {
    hasSlices: true,
  },
  widgets: {
    hasSlices: true,
  },
  pages: {
    hasSlices: true,
  },
};
